using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MutationSelectionText : MonoBehaviour
{
    public List<GameObject> promptTexts = new List<GameObject>();

    // Start is called before the first frame update
    void Start()
    {

        // promptTexts.Insert(GameObject.FindGameObjectsWithTag("Prompts"));

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
